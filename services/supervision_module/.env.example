# Supervision Module Service
PORT=4001
JWT_SECRET=replace-with-strong-secret
# Local Postgres example
DATABASE_URL=postgres://user:password@localhost:5432/amlguard

# Neon (managed PostgreSQL) example — requires SSL
# DATABASE_URL=postgresql://user:password@your-neon-host.neon.tech/amlguard?sslmode=require
LOG_LEVEL=dev
